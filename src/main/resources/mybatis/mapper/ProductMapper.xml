<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="mybaproject.rowonshop.global.dao.ProductMapper">

    <insert id="insertProduct" parameterType="mybaproject.rowonshop.global.dto.Product">

        INSERT INTO product (nums, id, category, productName, price, stocks, mainImage, miniText)
        VALUES (default, #{id}, #{category}, #{productName}, #{price}, #{stocks}, #{mainImage, jdbcType=BLOB}, #{miniText});

    </insert>


</mapper>